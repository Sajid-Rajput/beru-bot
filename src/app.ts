#!/usr/bin/env node
/**
 * app.ts — HTTP server + Bot entry point
 * TODO: Implement fully in T1.5 once config.ts is extended with DB_URL, REDIS_URL etc.
 * This will supersede main.ts and wire up:
 *   - Drizzle DB client (src/db/index.ts)
 *   - Hono server (src/server/index.ts)
 *   - Grammy bot in webhook mode (src/bot/index.ts)
 *   - Graceful shutdown
 */

// Placeholder — re-exports main until T1.5 refactor
export {}
